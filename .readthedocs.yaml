version: 2

build:
        os: ubuntu-20.04
        tools:
                python: "mambaforge-4.10"

conda:
        environment: mkdocs-env.yaml

mkdocs:
        configuration: mkdocs.yaml
        fail_on_warning: false
